# Construct the dataloader at different epochs  
loader = ... 

# Compute the distance for S-DANN T-DANN S-T at every epochs 

sdann_sims = []
tdann_sims = []

# Compute the S-T similarity 

st_sims = [] 

def get_checkpoint(epoch): 
    return 

# Compute the feature under the three different models 

for epoch in range(0,100,5): 

    fs = 

    ft = 

    fdann =

    sdann_sim = 
    tdann_sim = 

    print("s-dann sim: ; t-dann sim; s-t sim".format(,) )

np.save 